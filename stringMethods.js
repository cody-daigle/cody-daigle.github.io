/**
 * * String.at() - Method that takes in a positive or negative integer and returns a new string consisting of a single UTF-16 code at that specified offset.
 * String.charAt() - Method that takes in an integer and returns a new string consisiting of a single UTF-16 code at that given index.
 * String.charCodeAt() - Returns an integer between 0-65535 representingthe UTF-16 code at that given index.
 * String.codePointAt() - Returns a positive integer that represents the Unicode code point value of the character that that index given index.
 * String.concat() - Concatenates the string arguments and returns a new string.
 * String.endsWith() - Checks whether a string ends with the specified characters, returning a boolean.
 * String.fromCharCode() - A static method that returns a string created by the provided UTF-16 code units.
 * String.fromCodePoint() - This static method that returns a string created by the provided sequence of code units.
 * String.includes() - Performs a case-sensative search to check if a given string can be found within a string and returns a boolean.
 * String.indexOf() - performs a search through a string and at the first occurrence of the specified substring it will return the index.
 * String.isWellFormed() - Checks whether a string has any lone surrogates and will return a boolean based on the search.
 * String.lastIndexOf() - Similar to indexOf, but will return the index of the last occurence of a provided substring.
 * String.localeCompare() - Determines if a string comes before, after, or is the same as the provided string, in sort order.
 * String.match() - values retrieves the result of matching this string against
 * String.matchAll() - values returns an iterator of all results matching this string against a regular expression, including capturing groups.
 * String.normalize() - values returns the Unicode Normalization Form of this string.
 * String.padEnd() - values pads this string with a given string (repeated, if needed) so that the resulting string reaches a given length. The padding is applied from the end of this string.
 * String.padStart() - values pads this string with another string (multiple times, if needed) until the resulting string reaches the given length. The padding is applied from the start of this string.
 * String.raw() - This is a tag function for template literals.
 * String.repeat() - values constructs and returns a new string which contains the specified number of copies of this string, concatenated together.
 * String.replace() -values returns a new string with one, some, or all matches of a pattern replaced by a replacement. String or regex.
 * String.replaceAll() - values returns a new string with all matches of a pattern replaced by a replacement.
 * String.search() - values executes a search for a match between a regular expression and this string, returning the index of the first match in the string.
 * String.slice() - values extracts a section of this string and returns it as a new string, without modifying the original string.
 * String.split() - values takes a pattern and divides this string into an ordered list of substrings by searching for the pattern, puts these substrings into an array, and returns the array.
 * String.startsWith() - Checks whether a string starts with the specified characters, returning a boolean.
 * String.substring() - values returns the part of this string from the start index up to and excluding the end index
 * String.toLocaleLowerCase() - values returns this string converted to lower case, according to any locale-specific case mappings.
 * String.toLocaleUpperCase() - values returns this string converted to upper case, according to any locale-specific case mappings.
 * String.toLowerCase()- values returns this string converted to lower case.
 * String.toString() - values returns this string value.
 * String.toUpperCase() -  values returns this string converted to uppercase.
 * String.toWellFormed() - values returns a string where all lone surrogates of this string are replaced with the Unicode replacement character U+FFFD.
 * String.trim() - values removes whitespace from both ends of this string and returns a new string, without modifying the original string.
 * String.trimEnd() - values removes whitespace from the end of this string and returns a new string, without modifying the original string
 * String.trimStart() - values removes whitespace from the beginning of this string and returns a new string, without modifying the original string
 * String.valueOf() - values returns this string value.
 */